const DISCOUNT_TYPES = {
    amount: 'amount',
    percentage: 'percentage',
};

const SHIPMENT_STATUSES = {
    waiting: 'waiting',
    assigned: 'assigned',
    picked_up: 'picked_up',
    delivered: 'delivered',
};

module.exports = {
    DISCOUNT_TYPES,
    SHIPMENT_STATUSES,
};
